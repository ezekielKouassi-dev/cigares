export class Personne {

  // TODO: je dois revoir les propriétés de la classe personne
  constructor(
    public id?: number,
    public nom?: string,
    public prenoms?: string,
    public email?: string,
    public mot_de_passe?: string,
  ) {
  }
}
